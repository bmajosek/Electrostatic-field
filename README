To turn on a program on the CPU, you need to enter a blank argument, an argumentless call will turn on a program on the GPU. To stop the program, click the spacebar. The CPU program is very slow, it is recommended to specify a small number of particles <= 1000 in the GlobalVariables.hpp file. The .exe file can be found in x64/Release/Electrostatic.exe.
